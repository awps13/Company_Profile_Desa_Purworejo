import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ["images.unsplash.com", "assets.promediateknologi.id", "assetd.kompas.id", "lh3.googleusercontent.com", "s3-publishing-cmn-svc-prd.s3.ap-southeast-1.amazonaws.com"],
	},
};

export default nextConfig;
